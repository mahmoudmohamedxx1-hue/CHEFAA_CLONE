import IoTAdherenceMonitor from '../components/IoTAdherenceMonitor';

export default function IoTAdherencePage({ language }: { language: 'ar' | 'en' }) {
  return <IoTAdherenceMonitor language={language} />;
}
